<Window x:Class="Intergraph.AsiaPac.WpfApplication.UI.TextDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Enter value" Height="105" Width="346" ShowInTaskbar="False">
    <Grid Height="85" Width="321">
        <Grid.Resources>
            <ResourceDictionary>
                <ResourceDictionary.MergedDictionaries>
                    <ResourceDictionary Source="../Themes/ViewDialogLayout.xaml"/>
                    <ResourceDictionary Source="../Themes/GridLayout.xaml"/>
                </ResourceDictionary.MergedDictionaries>
            </ResourceDictionary>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBox Grid.Row="0" Grid.Column="0" Style="{StaticResource GridTextBox}" Text=""/>

        <StackPanel Grid.Column="0" Grid.Row="1">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Column="0" Grid.Row="1" >
                <Button Click="OK_Click" Style="{StaticResource ActionButton}">OK</Button>
                <Button Click="Cancel_Click" Style="{StaticResource ActionButton}">Cancel</Button>
            </StackPanel>
        </StackPanel>

    </Grid>
</Window>
