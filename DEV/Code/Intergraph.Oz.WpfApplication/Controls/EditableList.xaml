<UserControl x:Class="Intergraph.Oz.WpfApplication.Controls.EditableList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Height="300" Width="300">
    <Grid>
        <Grid.Resources>
            <ResourceDictionary>
                <ResourceDictionary.MergedDictionaries>
                    <ResourceDictionary Source="../Themes/ViewDialogLayout.xaml" />
                </ResourceDictionary.MergedDictionaries>
            </ResourceDictionary>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <ListBox Grid.Column="0" Grid.Row="0" Margin="0,0,0,0" Width="auto" Height="auto" Name="_list" />
        <StackPanel Grid.Column="0" Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Style="{StaticResource ActionButton}" Command="{Binding AddCommand}">Add</Button>
            <Button Style="{StaticResource ActionButton}" Command="{Binding EditCommand}">Edit</Button>
            <Button Style="{StaticResource ActionButton}" Command="{Binding RemoveCommand}">Remove</Button>
        </StackPanel>
    </Grid>
</UserControl>
