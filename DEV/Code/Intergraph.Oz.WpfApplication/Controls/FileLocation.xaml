<UserControl x:Class="Intergraph.Oz.WpfApplication.Controls.FileLocation"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Name="FileLocationControl" Height="auto" Width="auto" MinWidth="150">
    <Grid>
        <Grid.Resources>
            <ResourceDictionary>
                <Style TargetType="TextBox" x:Key="FileTextBox">
                    <Setter Property="Margin" Value="0 0 5 0"/>
                </Style>
                <Style TargetType="Button" x:Key="FileButton">
                    <Setter Property="Margin" Value="5 0 0 0"/>
                </Style>
            </ResourceDictionary>
        </Grid.Resources>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="32"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <TextBox x:Name="_filepath" Grid.Column="0" Grid.Row="0" Style="{StaticResource FileTextBox}" Text="{Binding FilePath, ElementName=FileLocationControl}" IsReadOnly="True"></TextBox>
        <Button Click="Button_Click" Grid.Column="1" Grid.Row="0" Style="{StaticResource FileButton}">
            <Image Width="16" Height="16" Source="../Resources/edit_find.png"/>
        </Button>
    </Grid>
</UserControl>
