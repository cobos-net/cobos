<Project ToolsVersion="4.0" DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <!-- 
  =============================================================================
  Build Definitions
  =============================================================================
  -->
  <PropertyGroup>
    <BinFolderNet35>$(ProjectDir)..\..\~bin\net35\</BinFolderNet35>
  </PropertyGroup>
  <!-- 
  =============================================================================
  Before Build  
  =============================================================================
  -->
  <Target Name="BeforeBuild">
  </Target>
  <!-- 
  =============================================================================
  After Build
  =============================================================================
  -->
  <Target Name="AfterBuild">
    <ItemGroup>
      <CobosBinaries Include="$(TargetPath)"/>
      <OracleBinariesX64 Include="$(ProjectDir)..\..\..\..\TOOLS\Oracle\net20\x64\*.dll"/>
    </ItemGroup>
    
    <MakeDir Directories="$(BinFolderNet35)" Condition="!Exists($(BinFolderNet35))"/>
    <MakeDir Directories="$(BinFolderNet35)x64"/>
    
    <Copy DestinationFolder="$(TargetDir)" SourceFiles="@(OracleBinariesX64)"/>
    <Copy DestinationFolder="$(BinFolderNet35)x64" SourceFiles="@(CobosBinaries)"/>
    <Copy DestinationFolder="$(BinFolderNet35)x64" SourceFiles="@(OracleBinariesX64)"/>
  </Target>
</Project>