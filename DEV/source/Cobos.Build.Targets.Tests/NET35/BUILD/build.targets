<Project ToolsVersion="4.0" DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <!-- 
  =============================================================================
  Build Definitions
  =============================================================================
  -->
  <PropertyGroup>
    <BinFolderNet35>$(ProjectDir)..\..\~bin\net35\</BinFolderNet35>
  </PropertyGroup>
  <!-- 
  =============================================================================
  Before Build  
  =============================================================================
  -->
  <Target Name="BeforeBuild">
  </Target>
  <!-- 
  =============================================================================
  After Build
  =============================================================================
  -->
  <Target Name="AfterBuild">
    <ItemGroup>
      <CobosBinariesX86 Include="$(BinFolderNet35)x86\*.*"/>
      <CobosBinariesX64 Include="$(BinFolderNet35)x64\*.*"/>
    </ItemGroup>    
    <Copy DestinationFolder="$(TargetDir)x86" SourceFiles="@(CobosBinariesX86)"/>
    <Copy DestinationFolder="$(TargetDir)x64" SourceFiles="@(CobosBinariesX64)"/>
  </Target>
</Project>