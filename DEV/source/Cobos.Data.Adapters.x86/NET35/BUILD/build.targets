<Project ToolsVersion="4.0" DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <!-- 
  =============================================================================
  Build Definitions
  =============================================================================
  -->
  <PropertyGroup>
    <BinFolderNet35>$(ProjectDir)..\..\~bin\net35\</BinFolderNet35>
  </PropertyGroup>
  <!-- 
  =============================================================================
  Before Build  
  =============================================================================
  -->
  <Target Name="BeforeBuild">
  </Target>
  <!-- 
  =============================================================================
  After Build
  =============================================================================
  -->
  <Target Name="AfterBuild">
    <ItemGroup>
      <CobosBinaries Include="$(TargetPath)"/>
      <OracleBinariesX86 Include="$(ProjectDir)..\..\..\..\TOOLS\Oracle\net20\x86\*.dll"/>
    </ItemGroup>
    
    <MakeDir Directories="$(BinFolderNet35)" Condition="!Exists($(BinFolderNet35))"/>
    <MakeDir Directories="$(BinFolderNet35)x86"/>
    
    <Copy DestinationFolder="$(TargetDir)" SourceFiles="@(OracleBinariesX86)"/>
    <Copy DestinationFolder="$(BinFolderNet35)x86" SourceFiles="@(CobosBinaries)"/>
    <Copy DestinationFolder="$(BinFolderNet35)x86" SourceFiles="@(OracleBinariesX86)"/>
  </Target>
</Project>